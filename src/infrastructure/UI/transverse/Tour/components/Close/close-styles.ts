import tw, { styled } from 'twin.macro'
import { Button } from '../../../../components'

export const CloseButton = styled(Button)`
    ${tw`h-auto pt-0 px-0 font-helvetica font-normal text-base leading-none`}
`
